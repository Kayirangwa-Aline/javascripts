//boolean
const price = 20;
console.log(price);

//you can store information by using javascript booleans like this:
// booleans can have only two values: true and false
const isOnSale = true;
console.log(isOnSale);
 //or

const isOnSales = false;
console.log(isOnSales);
// we can reassign variables with booleans
let isOnSale = true;
console.log(isOnSale);
isOnSale = false;
console.log(isOnSale);
 
//assign a value boolean
let isTrue = true;
console.log(isTrue);

//every single value in JavaScript can be transformed to a boolean value.
console.log(Boolean(100));
console.log(Boolean(2345));
console.log(Boolean(-0));
console.log(Boolean(null));
//Which of them become true and which become false?
console.log(Boolean(100));
console.log(Boolean(1));
console.log(Boolean(0));
console.log(Boolean(-5));

//convert number to boolean
let num = 5;
num = 0;
const bool = Boolean(num);
console.log(bool);

